# codex1

这是一个用于 Codex / ChatGPT 代码练习的 Python 项目仓库。

## 项目结构

```text
.
├── README.md
├── .gitignore
├── main.py
└── requirements.txt
```

## 运行方式

```bash
python main.py
```

## 依赖安装

当前项目暂未添加第三方依赖。如果后续需要安装依赖，可以执行：

```bash
pip install -r requirements.txt
```
